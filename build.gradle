plugins {
    id("java")
    id("maven-publish")
    id("gronk").version("latest.release")
}

group("auoeke")
version("0.0.0")

javaVersion(17)

gronk {
    uncheck()
}

sourceSets {
    test {
        resources.srcDir("test/cin")
    }
}

repositories {
    mavenCentral()
    maven("maven.auoeke.net")
}

dependencies {
    testImplementation("org.junit.jupiter:junit-jupiter-api")
    testRuntimeOnly("org.junit.jupiter:junit-jupiter-engine")
}

test {
    useJUnitPlatform()
}
